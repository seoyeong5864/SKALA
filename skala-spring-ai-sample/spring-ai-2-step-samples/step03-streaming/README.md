# STEP 03 - Streaming / SSE

```bash
./gradlew :step03-streaming:bootRun
```

브라우저:

```text
http://localhost:8103/
```

CLI:

```bash
curl -N "http://localhost:8103/api/chat/stream?message=RAG를%20설명해줘"
```
