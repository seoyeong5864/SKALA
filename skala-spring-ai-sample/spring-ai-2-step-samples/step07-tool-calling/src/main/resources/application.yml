# ============================================================
# STEP 07 - Tool Calling
# Tool은 코드(@Tool 애노테이션)로 정의하므로 추가 설정이 없다.
#
# 참고: Tool Calling은 [모델 호출 → Tool 실행 → 모델 재호출] 구조라
# 한 번의 요청에 API 호출이 2회 이상 발생한다. 비용·지연을 감안하자.
# ============================================================
server:
  port: 8107

spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        model: gpt-5-mini
